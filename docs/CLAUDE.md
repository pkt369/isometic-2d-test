# CLAUDE.md

Claude 전용 진입 파일.

이 저장소의 모든 운영 규칙은 `AGENTS.md`가 단일 기준(SSOT)이다.
**작업 시작 전 반드시 `AGENTS.md`를 먼저 읽고 그대로 따른다.**

여기에는 규칙을 요약, 중복 기재하지 않는다. 요약본이 따로 있으면 본문과 어긋나(drift) 치명 규칙이 누락된다. 규칙은 항상 `AGENTS.md`에서 직접 확인한다.
